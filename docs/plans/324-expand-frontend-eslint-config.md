# 324 — Expand frontend ESLint config with type-checked and library rules

Issue: [#324](https://github.com/emsqrd/f1fantasyapp/issues/324)

Expand `web/`'s ESLint config from `tseslint.configs.recommended` to core + type-checked rules plus usage-matched library plugins, and give `e2e/` its first ESLint config. All packages verified against the live registry as ESLint-10-compatible.

## Approach

- **`recommendedTypeChecked`, not `strictTypeChecked`.** `strict`'s `no-unnecessary-condition` assumes `noUncheckedIndexedAccess`, which is **off** in `tsconfig.app.json`, so it would mis-flag index access.
- **Verification per commit:** `web:format:check`, `web:lint`, `web:build`, `web:test` all green (this is the pre-commit hook's own sequence). For the e2e commit, additionally `e2e:lint` green and the CI reorder correct. There are no new unit tests — for config work, a green lint/build _is_ the test.

## Config decisions baked in

- **`js.configs.recommended`** added to the extends array (core rules like `no-constant-binary-expression`, `use-isnan`, `valid-typeof` that tseslint's preset does not include).
- **`eslint-config-prettier`** added **last** in the array (disables any formatting rules; Prettier + `@trivago/sort-imports` own formatting).
- **`projectService: true` + `tsconfigRootDir`** under `languageOptions.parserOptions` to power the type-checked rules. Coverage is already complete: `src/**` → `tsconfig.app.json`, `vite.config.ts` → `tsconfig.node.json`. JS config files (`eslint.config.js`, `prettier.config.js`) aren't matched by the `**/*.{ts,tsx}` block, so they aren't type-checked.
- **`@typescript-eslint/switch-exhaustiveness-check`** enabled explicitly — it ships with typescript-eslint but no preset turns it on.
- **`import-x/no-cycle` only** from `eslint-plugin-import-x`, with `eslint-import-resolver-typescript` wired via `settings['import-x/resolver-next']` — the default node resolver can't follow the `@/` alias, which is nearly every import in the codebase. Skip `import/order` — Prettier owns import ordering.
- **Testing-library + Vitest plugins scoped to test files** — a dedicated config block with `files: ['src/**/*.{test,spec}.{ts,tsx}', 'src/setupTests.ts', 'src/tests/**/*.{ts,tsx}']` so their rules don't evaluate production code.
- Keep the existing `ignores` (`dist`, `coverage`, `src/components/ui` — vendored shadcn).
- Keep `react-hooks` as-is apart from the `exhaustive-deps` severity bump above.

### Drive-by cleanups (folded in, tracked on the issue)

- **Remove the custom `@typescript-eslint/no-unused-vars` block.** The preset already ships it at `error`; the `^_$` exemption is idle (grep-confirmed: no `catch (_)`, and the `(_, i)` callbacks pass on the default `after-used`). Removing it keeps the rule at `error` with defaults — a no-op on today's tree. Side effect going forward: an intentionally-ignored caught error is written `catch {`, not `catch (_)`.
- **Bump `react-refresh/only-export-components` `warn` → `error`** (keep `allowConstantExport: true` and the plugin registration). Its consequence is dev-only (a Fast Refresh full reload), but a non-blocking nudge protects nothing; the fix (split the non-component export out) is cheap and structurally good. Keep the plugin registration line — it's the rule's only activation.

### Deliberately excluded (see issue for full reasoning)

`eslint-plugin-unicorn` (curation cost), `eslint-plugin-sonarjs` (only unique bug-catch over `js.configs.recommended` is the low-frequency `no-all-duplicated-branches`; not worth a 3.6 MB dep + preset curation), `eslint-plugin-react` (no ESLint 10 support; moot under React 19's automatic JSX runtime), `eslint-plugin-jsx-a11y` (no ESLint 10 support — a **known, tracked gap** vs. the WCAG 2.1 AA goal, not an oversight), `eslint-plugin-jest-dom` (marginal over testing-library), original `eslint-plugin-import` (superseded by `import-x`).

## Commit sequence

Self-contained, approval-gated commits; foundation first. Each is green on its own.

### 1. Core rules + config cleanups

- Add `js.configs.recommended`; install `eslint-config-prettier` and add it last.
- Remove the `no-unused-vars` custom block.
- Bump `react-refresh/only-export-components` to `error`.
- Bump `react-hooks/exhaustive-deps` to `error` (the recommended preset ships it at `warn`). A stale dependency array is a real bug class, not an advisory nit; zero violations and zero disable comments today, so the bump is free; intentional dep omissions going forward take an explicit `eslint-disable-next-line`.
- Add `^web/eslint\.config\.js` to the pre-commit `WEB_CHANGED` pattern — every later commit edits that file, and it currently triggers no web checks.
- Fix whatever `js.configs.recommended` + the react-refresh bump surface.

### 2. Type-checked preset (atomic)

- Switch `tseslint.configs.recommended` → `recommendedTypeChecked`; add `projectService: true` + `tsconfigRootDir`; add `switch-exhaustiveness-check`.
- Introduce the test-files config block with `@typescript-eslint/unbound-method: 'off'` — `expect(obj.method)` is the suite's idiomatic assertion style and a known false positive for this rule. Commits 5–6 later add their plugins to the same block.
- The whole type-checked rule set turns on at once (a preset can't be adopted incrementally), so this commit fixes every violation the type-checked rules surface.

### 3. `@tanstack/eslint-plugin-query`

- Install; enable its recommended (`exhaustive-deps` for the hand-rolled `queryOptions` factories). Fix any violations it surfaces.

### 4. `@tanstack/eslint-plugin-router`

- Install; enable `create-route-property-order` (+ recommended). Fix any violations it surfaces.

### 5. `eslint-plugin-testing-library`

- Install; enable its flat recommended, **scoped to test files**. Fix any violations it surfaces.

### 6. `@vitest/eslint-plugin`

- Install; enable its flat recommended at the preset's authored severities, **scoped to test files**. Fix any violations it surfaces.

### 7. `eslint-plugin-import-x`

- Install `eslint-plugin-import-x` + `eslint-import-resolver-typescript`; enable **only** `import-x/no-cycle` with the TS resolver in `settings['import-x/resolver-next']`. Fix any violations it surfaces.

### 8. e2e ESLint config + CI/pre-commit wiring

- **`e2e/eslint.config.js`** mirroring web's language-level bar: `js.configs.recommended` + `recommendedTypeChecked` (`projectService: true` + `tsconfigRootDir` → `e2e/tsconfig.json`, which already includes all 17 files) + `switch-exhaustiveness-check` + `import-x/no-cycle` + `eslint-config-prettier` last, **plus `eslint-plugin-playwright`** recommended (`no-wait-for-timeout` enforces "never sleep"; `no-focused-test` catches committed `.only`). `ignores`: `playwright-report/`, `test-results/`, `supabase/`. No React-ecosystem plugins and no TS resolver — e2e has no path aliases.
- **Install (e2e devDeps):** `eslint`, `@eslint/js`, `typescript-eslint`, `eslint-plugin-playwright`, `eslint-plugin-import-x`, `eslint-config-prettier`.
- **Scripts:** add `"lint": "eslint ."` to `e2e/package.json`; add `"e2e:lint": "cd e2e && npm run lint"` to root `package.json`.
- **CI (`.github/workflows/ci.yml`, e2e job):** move the e2e `npm ci` step **above** `supabase start`, then add a "Lint e2e" step right after it — a lint failure short-circuits before the 15-min stack spins up.
- **Pre-commit (`.husky/pre-commit`):** add an `E2E_CHANGED` branch (`git diff --cached --name-only | grep -E '^e2e/'`) that runs `e2e:format:check` + `e2e:lint` only — **never** the e2e tests (they need the Supabase stack; linting is seconds).
- Fix any violations the type-checked + Playwright rules surface (the async fixtures — `pg`/`fetch`/Supabase in `fixtures/` — are where `no-floating-promises` earns its keep).

## Out of scope

- Enforcing absolute-imports (`no-restricted-imports` / `import-x/no-relative-parent-imports`) — needs an ~80-site cleanup first; separate issue.
- Adopting `jsx-a11y` — blocked on upstream ESLint 10 support; revisit when it ships.
- Any formatting/stylistic rules — Prettier owns them.

## Reference: violations surfaced by the type-checked preset (commit 2)

Snapshot of `web:lint` immediately after the `recommendedTypeChecked` switch, before any fixes: **186 violations across 12 rules**. `switch-exhaustiveness-check` surfaced zero.

The Solution column records the researched, decided fix per rule. Two are config decisions (the `only-throw-error` allow-list, `no-misused-promises` `attributes: false`), both vendor-documented; every other rule stays on everywhere — no test-block carve-outs, no inline disables — with fixes at the `any` source. Two root-cause fixes (`ImportMetaEnv` augmentation, generic route-tree builders) clear violations across several rules at once. An audit of every flagged async handler and floating promise found no unhandled-rejection bugs behind the flagged shapes.

| Rule                            | What it does                                                                                                | Count | Files                                                                                                                                                                                                                                                                                                                              | Example                                                                                                    | Solution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `no-unsafe-assignment`          | Disallows assigning an `any` value to a variable or property                                                | 39    | ConstructorPicker.tsx, DriverPicker.tsx, api.test.ts, api.ts, authStore.test.ts, route-guards.test.ts, supabase.ts, main.tsx, userProfileService.test.ts, leaderboard.integration.test.tsx, league-membership.integration.test.tsx, leagues.integration.test.tsx, team-lineup.integration.test.tsx, view-team.integration.test.tsx | `ConstructorPicker.tsx:37` — `const slots: (Constructor \| null)[] = Array(CONSTRUCTOR_SLOTS).fill(null);` | Fix the `any` at its source in app code: (1) augment `ImportMetaEnv` in `vite-env.d.ts` with the app's `readonly VITE_…: string` keys — Vite's documented pattern; the bare `vite/client` reference types every `import.meta.env` read as `any` today, which is also the root of most `no-unsafe-argument` hits. (2) `Array<Constructor \| null>(CONSTRUCTOR_SLOTS).fill(null)` — the typed `Array` constructor overload. (3) `const problemDetails: unknown = JSON.parse(errorBody)` — the rule explicitly permits `any` → `unknown`. In tests the rule stays on — no carve-out. Its ~19 matcher hits resolve as real fixes: the 18 `expect.any()` type-only assertions in userProfileService.test.ts are weak assertions (deterministic fixture in, type-of-field checks out, each duplicating an adjacent `toEqual` test) — delete those three test cases; the one legitimate matcher (authStore.test.ts:217, pinning the forwarded `emailRedirectTo`) moves to argument position where the rule doesn't fire — `expect(vi.mocked(supabase.auth.signUp).mock.calls[0][0].options).toEqual(expect.objectContaining({…}))` (matchers only trip this rule as object-literal properties, verified against api.test.ts's four unflagged argument-position uses); type the implicit-`any` `vi.fn` param in route-guards.test.ts:17. |
| `no-unsafe-member-access`       | Disallows reading properties off an `any` value                                                             | 28    | api.ts, account.integration.test.tsx, leaderboard.integration.test.tsx, league-invite-dialog.integration.test.tsx, league-membership.integration.test.tsx, leagues.integration.test.tsx, team-lineup.integration.test.tsx, view-team.integration.test.tsx                                                                          | `api.ts:78` — `if (problemDetails.detail) {`                                                               | App (`api.ts`): narrow the parsed Problem Details from `unknown` with a property/type guard before reading `.detail` — the maintainers' documented JSON-boundary posture (`unknown` + narrowing, or a schema library); a bare `as ProblemDetails` cast is what strict's `no-unsafe-type-assertion` exists to flag. Tests: the root cause is `routeTreeBuilders.tsx` typing parents as `AnyRoute`, which erases `RouterContext` and makes every descendant loader's `context` an `any` — make the builders generic (`<TParent extends AnyRoute>(parent: TParent)`) so context typing flows again. The rule stays on in tests; the one leftover (the `delete (navigator as any).clipboard` restore in league-invite-dialog) swaps `any` for a structural cast: `(navigator as { clipboard?: unknown })`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `only-throw-error`              | Requires thrown values to be `Error` objects                                                                | 27    | JoinInvite.tsx, Leaderboard.tsx, League.tsx, Team.tsx, route-guards.ts, router.tsx, auth-confirm.integration.test.tsx, join-invite.integration.test.tsx, leaderboard.integration.test.tsx, league-invite-dialog.integration.test.tsx, leagues.integration.test.tsx, view-team.integration.test.tsx                                 | `JoinInvite.tsx:34` — `if (!preview) throw notFound();`                                                    | Config, not code — TanStack Router's own ESLint docs publish the fix: `'@typescript-eslint/only-throw-error': ['error', { allow: [{ from: 'package', package: '@tanstack/router-core', name: 'Redirect' }, { from: 'package', package: '@tanstack/router-core', name: 'NotFoundError' }] }]`. All 27 are `throw redirect()` / `throw notFound()` — TanStack's designed control flow — and the installed `router-core` types confirm both names.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `no-misused-promises`           | Flags Promises used where a void return is expected (e.g. async handlers on event props) or in conditionals | 26    | Account.tsx, AccountMenu.tsx, AvatarUpload.tsx, BrowseLeagues.tsx, CreateLeague.tsx, CreateTeam.tsx, MyLeaguesList.tsx, JoinInvite.tsx, LandingPage.tsx, Layout.tsx, League.tsx, LeagueList.tsx, RouteErrorComponent.tsx, CheckEmailNotice.tsx, ConfirmEmailNotice.tsx, SignInForm.tsx, SignUpForm.tsx, authStore.test.ts          | `Account.tsx:62` — `onAvatarChange={handleAvatarChange}`                                                   | 25 of 26 are async handlers on JSX props: set `checksVoidReturn: { attributes: false }` — the rule docs' selective opt-out for exactly this shape. Documented tradeoff: a rejection inside the handler floats unobserved — but an audit of all 25 handlers found zero that let one escape (13 catch and surface errors via `InlineError`/toasts; 11 can't reject — bare `navigate()`, Query `refetch()`), and Sentry's global handlers capture any future stray rejection. The rule flags handler shape regardless of internal handling, so per-site error handling could never satisfy it. Hardening candidate independent of the rule: a defensive catch in `ConfirmEmailNotice.handleContinue`, where a non-`AuthError` throw would strand the button in "Confirming…". The docs' per-site alternative — wrapping each prop in a sync `() => void handle()` arrow — restates the same intent 25 times without handling anything more. All other checks (`arguments`, variables, conditionals) stay on. The one non-attribute hit (`authStore.test.ts:61`) is a genuine mismatch: widen `authCallback`'s declared return to Supabase's actual callback contract, `void \| Promise<void>`.                                                                                                                                      |
| `no-unsafe-call`                | Disallows calling a value typed `any` as a function                                                         | 21    | api.test.ts, account.integration.test.tsx, leaderboard.integration.test.tsx, league-membership.integration.test.tsx, leagues.integration.test.tsx, team-lineup.integration.test.tsx, view-team.integration.test.tsx                                                                                                                | `api.test.ts:18` — `withScope: vi.fn((callback) => callback({ setTag: vi.fn(), ... }))`                    | All test-file hits, two roots: the `AnyRoute` erasure (fixed by the generic builders above — clears every `context.queryClient` call) and the untyped `withScope` callback param in api.test.ts's `vi.mock` factory — type the param, or use the typed factory form `vi.mock(import('…'), async (importOriginal) => …)`. The rule stays on in tests: after these fixes it has zero hits and keeps flagging future untyped mocks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `no-floating-promises`          | Requires Promises to be awaited, `.catch`-ed, or explicitly `void`-ed                                       | 17    | AccountMenu.tsx, AppSidebar.tsx, AvatarUpload.tsx, BrowseLeagues.tsx, CreateLeague.tsx, CreateTeam.tsx, JoinInvite.tsx, Layout.tsx, CheckEmailNotice.tsx, authStore.ts                                                                                                                                                             | `AccountMenu.tsx:45` — `navigate({ to: '/account' });`                                                     | Per-site `void` operator — the rule's documented marker for intentional fire-and-forget (`void queryClient.invalidateQueries(…)`, menu/logo `navigate`s); prefer `await` where it's also semantically truer — the BrowseLeagues/CreateTeam navigates sit in async handlers whose `finally`/`isSubmitting` currently flip before navigation completes. Audit of all 17 sites: none loses a rejection silently — `invalidateQueries` doesn't reject by contract, `navigate` failures land in router error boundaries, and `authStore.ts:146`'s init seed has a redundant safety net (auth-js emits `INITIAL_SESSION` even on error, so `loading` still clears — no init hang). No blanket allowance: neither typescript-eslint nor TanStack documents `navigate` as known-safe, so `allowForKnownSafeCalls` isn't source-supported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `no-unnecessary-type-assertion` | Flags `as` assertions that don't change the expression's type                                               | 9     | AvatarUpload.test.tsx, OtpInput.test.tsx, PositionDelta.tsx, api.test.ts, auth-confirm.integration.test.tsx, matchMedia.ts                                                                                                                                                                                                         | `AvatarUpload.test.tsx:93` — `screen.getByLabelText(/upload avatar image/i) as HTMLInputElement`           | Don't take the autofix blindly on the testing-library ones: the `as` target is what's inferring the query's generic (`getByLabelText<T>` infers `T` from it), so plain removal widens the element back to `HTMLElement` and breaks the compile. Decision: the explicit generic — `screen.getByLabelText<HTMLInputElement>(…)`. A `const input: HTMLInputElement = …` annotation compiles identically via the same contextual inference, but this codebase's uniform idiom for a type parameter inference can't solve is the explicit type argument — `useRef<HTMLInputElement>(null)` in these same components, `useState<string \| null>(null)`, `createRootRouteWithContext<RouterContext>()`, `apiClient.get<Team>(…)`, `vi.fn<Auth['signUp']>()` — while annotation-driven inference appears nowhere; the three `as` casts are the lone deviation. The rest are true no-ops: drop `value as number` (PositionDelta), the `as typeof window.matchMedia` layer (matchMedia.ts), and `useSearch(…) as {…}` (auth-confirm); replace `vi.fn() as MockedFunction<typeof fetch>` with `vi.fn<typeof fetch>()` (Vitest's single-generic form).                                                                                                                                                                                       |
| `no-unsafe-argument`            | Disallows passing an `any` value as an argument to a typed parameter                                        | 8     | api.test.ts, supabase.ts, main.tsx, league-membership.integration.test.tsx, team-lineup.integration.test.tsx, view-team.integration.test.tsx                                                                                                                                                                                       | `api.test.ts:82` — `vi.stubEnv('VITE_F1_FANTASY_API', originalValue);`                                     | Keep it on everywhere — even typescript-eslint's repo leaves this one on in its test globs. All 8 disappear via the two root-cause fixes above: the `ImportMetaEnv` augmentation (supabase.ts, main.tsx, `vi.stubEnv`) and the generic route-tree builders (the `team.id` arguments).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `no-unsafe-return`              | Disallows returning an `any` value from a function with a typed return                                      | 5     | Layout.test.tsx, SignInForm.test.tsx, SignUpForm.test.tsx, api.test.ts                                                                                                                                                                                                                                                             | `Layout.test.tsx:14` — `useMatches: () => mockUseMatches(),`                                               | Test-only — untyped `vi.fn()` results returned through hand-rolled `@tanstack/react-router`/`useAuth` module mocks (Layout, SignInForm, SignUpForm tests). Keep the rule on and type the mocks (`vi.fn<typeof useMatches>()`), making mock drift compile-visible. Those hand-mocks are also the pattern web/CLAUDE.md calls integration-layer territory — re-layering those tests is a separate issue; typed mocks satisfy the rule now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `require-await`                 | Flags `async` functions that contain no `await`                                                             | 2     | AvatarUpload.tsx, SignUpForm.test.tsx                                                                                                                                                                                                                                                                                              | `AvatarUpload.tsx:72` — `const handleFileSelect = async (event) => {`                                      | Remove `async` from both — neither body awaits and neither contract requires a Promise (`handleFileSelect` calls the sync mutate `uploadAvatar`; a `vi.mock` factory may be synchronous). Dropping it on `handleFileSelect` also clears that file's `no-misused-promises` hit on the `onChange` prop. The extension rule already exempts `async` functions that return promise-typed values, so no `Promise.resolve` shims are needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `restrict-plus-operands`        | Requires both operands of `+` to be numbers or strings                                                      | 2     | api.test.ts, leagueService.test.ts                                                                                                                                                                                                                                                                                                 | `api.test.ts:24` — `acc + str + (values[i] \|\| '')`                                                       | Same two lines as `no-base-to-string`, one fix: `acc + str + String(values[i] ?? '')` in the Sentry `fmt` stubs. `values: unknown[]` is the real tagged-template contract; the violation is the `unknown` operand (never allowed by this rule, even under recommended's permissive defaults), and `String(…)` yields a plain `string`. These two rules aren't in the disabled quartet, so the code fix is needed despite living in test files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `no-base-to-string`             | Flags stringifying values that would produce `[object Object]`                                              | 2     | api.test.ts, leagueService.test.ts                                                                                                                                                                                                                                                                                                 | `api.test.ts:24` — same expression as above                                                                | Fixed by the same `String(values[i] ?? '')` wrap — `String` on `unknown` passes under the rule's defaults (`checkUnknown: false`); the current `values[i] \|\| ''` is what would stringify a potential object as `[object Object]`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
